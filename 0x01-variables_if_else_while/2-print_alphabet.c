#include <stdio.h>
/**
 * main - A program that prints alphabets using putchar function
 * Return: an Alphabet character
 */
int main(0)
{
char c;
c = 'a';
while (c <= 'z')
{
	putchar("%c", c);
	c++
}
return (0);
