#include <stdio.h>
/**
 * main - A program that prints the size of types
 * Return: Always 0 (Success)
 */
int main(void)
{
printf(sizeof(char));
printf(sizeof(int));
printf(sizeof(long int));
printof(sizeof(long long int));
printof(sizeof(float));
return (0);
}
