#include "main.h"
/**
 * int _islower(int c) - a function that checks characters
 * Return: 1 if lowercase 0 otherwise
 */
int _islower(int c)
{
char r;
if (r >= '97' || r <= '122')
{
return (1);
}
else
{
	return (0);
}
}
