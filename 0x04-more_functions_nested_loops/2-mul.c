#include "main.h"
/**
 * mul - A function that multiply two integers
 * @a: the first integer
 * @b: the second integer
 * Return: 0 successful
 */
int mul(int a, int b)
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
